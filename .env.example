# NEXORA AI — Server Environment Configuration
PORT=5000
NODE_ENV=development

# JWT Authentication
JWT_SECRET=nexora_jwt_super_secret_production_key_2026_xyz987
JWT_EXPIRES_IN=7d

# Database (Optional: if empty, Nexora AI seamlessly uses the built-in embedded persistent engine)
MONGODB_URI=

# AI Providers (Optional: if empty, Nexora AI uses the built-in intelligent semantic engine)
GEMINI_API_KEY=
OPENAI_API_KEY=
DEFAULT_AI_PROVIDER=gemini

# Storage
MAX_FILE_SIZE_MB=50
UPLOAD_DIR=./uploads
EXPORTS_DIR=./exports

# Super Admin Initial Credentials
SUPER_ADMIN_NAME="Nexora Administrator"
SUPER_ADMIN_EMAIL=admin@nexora.ai
SUPER_ADMIN_PASSWORD=NexoraAdmin2026!
